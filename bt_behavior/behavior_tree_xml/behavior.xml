<?xml version="1.0"?>
<root main_tree_to_execute="BehaviorTree">
    <!-- ////////// -->
    <BehaviorTree ID="BehaviorTree">
        <Sequence>
            <Action ID="GetPoint" goal="{waypoint}"/>
            <Action ID="Move" goal="{waypoint}"/>
            <Action ID="IsLastPoint"/>
        </Sequence>
    </BehaviorTree>
    <!-- ////////// -->
    <TreeNodesModel>
        <Action ID="GetPoint">
            <output_port name="goal"/>
        </Action>
        <Action ID="Move">
            <input_port name="goal"/>
        </Action>
        <Action ID="IsLastPoint"/>
    </TreeNodesModel>
    <!-- ////////// -->
</root>
